function add(a, b) {
    // return a+b
}
 function sub(a,b) {
    // if (a < b) {
    //     return a -b
    // } else {
    //     return b -a
    // }
 }

 function sum(num) {
    for (let i = 0; i < array.length; i++) {
        const element = array[i];
    }
 }
 